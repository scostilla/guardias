export const environment = {
  production: true,
  apiUrl: ''  // Vacío para rutas relativas.
  //apiUrl: 'http://localhost:8080'  // para desarrollo //para desarrollo es una variable que se usa con localhost, 
  // pero para produccion es vacio lo que se usa, se comenta y descomenta segun el uso
  
};
